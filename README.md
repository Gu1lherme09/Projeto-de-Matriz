# Projeto-de-Matriz
Um sotware para resolver matrizes 

Passo a passo para rodar o projeto:

1 - Instalar Python
2 - Criar o ambiente virtual venv na raiz do projeto utilizando o comando python -m venv venv
3 - Rodar o comando para ativar o ambiente virtual venv\Scripts\Activate
4 - Realizar o comando pip install -r requirements.txt onde está o arquivo requirements.txt
5 - Acessar o local onde está o arquivo manage.py e rodar o seguinte comando: python manage.py runserver
6 - A aplicação estará rodando na seguinte URL -> http://127.0.0.1:8000/ em qualquer navegador
